#include<stdio.h>
#include<ctype.h>
#include<math.h>


int  main() {

    int n, m, a, b, c;

    //scanf("%2d %*d %5d", &a, &b, &c);

    //scanf("%d %d", &m, &n);

    //printf("%d %d %d %d %d\n", a, b, c, m, n);

    scanf("%d-%d", &a, &b);
    printf("%d %d\n", a, b);

    return 0;
}
